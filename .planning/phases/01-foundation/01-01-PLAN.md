---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, server.js, public/index.html, public/app.js, public/style.css]
autonomous: true
requirements: [AUTH-02, DISP-04]
must_haves:
  truths:
    - "User can access root URL (/) and see a 'Connected' status"
    - "Server logs show WebSocket connection establishment"
    - "Manual 'draw' emit from server console updates participant UI instantly"
  artifacts:
    - path: "server.js"
      provides: "Unified Express + Socket.io server"
    - path: "public/index.html"
      provides: "Base participant UI"
    - path: "public/app.js"
      provides: "Socket.io client with reconnect logic"
  key_links:
    - from: "server.js"
      to: "Socket.io"
      via: "http server attachment"
    - from: "public/app.js"
      to: "server.js"
      via: "Socket.io handshake"

---

<objective>
Establish the walking skeleton: a Node.js server serving a static participant page with a live Socket.io link.

Purpose: Validate the real-time transport and deployment compatibility early.
Output: Working ping-pong link between server and client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Init & Express/Socket.io Setup</name>
  <files>package.json, server.js</files>
  <action>
    Initialize npm project with dependencies: express@5, socket.io, dotenv. 
    Create a unified `server.js` that:
    1. Boots Express on `process.env.PORT || 3000`.
    2. Attaches Socket.io to the HTTP server.
    3. Serves static files from `public/`.
    4. Implements a heartbeat (ping/pong) per PITFALL 7.
    5. Logs "Bingo Server running on port X".
  </action>
  <verify>
    <automated>node server.js & sleep 2 && curl -s localhost:3000 | grep -q "Bingo" && pkill -f server.js</automated>
  </verify>
  <done>Server boots and accepts connections on a single port.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Participant Skeleton UI & Socket Link</name>
  <files>public/index.html, public/app.js, public/style.css</files>
  <behavior>
    - Browser opens `/`: Shows "Status: Connecting..."
    - Socket connects: Status changes to "Connected"
    - Server emits `numberDrawn`: UI displays the number prominently
    - Socket disconnects: Status changes to "Disconnected (Reconnecting...)"
  </behavior>
  <action>
    Create a minimal participant page. 
    `app.js` must implement exponential backoff reconnection per PITFALL 4.
    Use Socket.io's auto-reconnect but add a visual state indicator.
    Server should have a temporary `setInterval` to push a random number every 5s for verification.
  </action>
  <verify>
    <automated>grep -q "socket.io.js" public/index.html && grep -q "io()" public/app.js</automated>
  </verify>
  <done>Participant can see real-time updates pushed from the server.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Walking Skeleton (Express + Socket.io + Participant View)</what-built>
  <how-to-verify>
    1. Run `node server.js`
    2. Open `http://localhost:3000` in two separate tabs.
    3. Verify both show "Status: Connected".
    4. Wait for the server to push a random number (verify it appears in both tabs).
    5. Kill the server: Verify tabs show "Disconnected".
    6. Restart server: Verify tabs automatically reconnect.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Server (WS) | Untrusted participant messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | server.js | mitigate | Only listen for 'sync' from participants; ignore all mutation events (reserved for host in Phase 2) |
| T-01-02 | DoS | server.js | mitigate | Implement Socket.io heartbeat to clear stale connections (Pitfall 7) |
</threat_model>

<verification>
Manual multi-tab sync check.
</verification>

<success_criteria>
1. Root URL serves index.html.
2. Socket.io handshake succeeds on same port.
3. Server can push events to all connected clients.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation/01-01-SUMMARY.md
</output>
