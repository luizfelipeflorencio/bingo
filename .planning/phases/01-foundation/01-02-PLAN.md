---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-foundation-01]
files_modified: [server.js, public/app.js]
autonomous: true
requirements: [SYNC-01, SYNC-03]
must_haves:
  truths:
    - "New client joining mid-game sees current drawn numbers"
    - "Server restart resets client UI"
  artifacts:
    - path: "server.js"
      provides: "In-memory state + SYNC event"
  key_links:
    - from: "server.js"
      to: "public/app.js"
      via: "SYNC message on connection"

---

<objective>
Implement state synchronization to ensure all clients stay aligned, even if they join late or reconnect.

Purpose: Solve Pitfall 1 (No state snapshot) and Pitfall 2 (LocalStorage authority).
Output: Deterministic state sync between server and any new connection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-01-PLAN.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server-Side In-Memory State & Sync</name>
  <files>server.js</files>
  <behavior>
    - Initialize `gameState = { drawnNumbers: [], lastDrawn: null }`
    - On new Socket connection: Server emits `SYNC` event with the full `gameState`
    - Update temporary test loop: Push a number to `drawnNumbers` before emitting
  </behavior>
  <action>
    Implement the handshake contract: `SYNC` must be the first message a client receives. 
    Wrap `io.emit` calls in try/catch per performance best practices.
  </action>
  <verify>
    <automated># Start server, connect client, check if 'SYNC' received immediately</automated>
  </verify>
  <done>Server maintains authoritative state and pushes it to every new connection.</done>
</task>

<task type="auto">
  <name>Task 2: Client-Side Reconciler (LocalStorage vs SYNC)</name>
  <files>public/app.js</files>
  <action>
    Implement the authority hierarchy per PITFALL 2:
    1. On `SYNC`: Unconditionally overwrite local state with server state.
    2. Save current state to `localStorage` after every server update.
    3. On Page Load (before socket): Populate UI from `localStorage` (optimistic UX).
    4. On Socket Connect: `SYNC` will overwrite local cache if server restarted.
  </action>
  <verify>
    <automated># Manual: Draw 5, reload page, see 5. Restart server, reload page, see 0.</automated>
  </verify>
  <done>Client handles server restarts and page refreshes without state drift.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Mid-Game Sync & Reconnection Logic</what-built>
  <how-to-verify>
    1. Start server.
    2. Open Tab A. Wait for 3 numbers to be "drawn" (simulated).
    3. Open Tab B.
    4. Verify Tab B immediately shows the same 3 numbers as Tab A (SYNC working).
    5. Restart the server.
    6. Verify both tabs clear their lists (Server Authority working).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server -> Client | State data leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Info Disclosure | server.js | mitigate | Only sync 'drawnNumbers'. Do not include sensitive host info in the participant SYNC payload. |
</threat_model>

<verification>
Simulated mid-game join test.
</verification>

<success_criteria>
1. Late-joining clients receive the full history of drawn numbers.
2. Server state unconditionally overwrites client LocalStorage on connection.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation/01-02-SUMMARY.md
</output>
