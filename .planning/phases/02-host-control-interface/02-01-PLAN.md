---
phase: 02-host-control-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["server.js", "public/host.html", "public/host.js", ".env"]
autonomous: true
requirements: ["AUTH-01"]
must_haves:
  truths:
    - "Host can only connect to WebSocket if the correct secret key is provided"
    - "Unauthorized connection attempts to host-only events are rejected"
  artifacts:
    - path: "public/host.html"
      provides: "Host entry point"
    - path: "public/host.js"
      provides: "Host-specific connection logic"
  key_links:
    - from: "public/host.js"
      to: "server.js"
      via: "Socket.io auth handshake"
---

<objective>
Implement secure host authentication via Socket.io handshake middleware and the `HOST_SECRET` environment variable. Establish the specialized host entry point.

Purpose: Prevent unauthorized users from controlling the game while avoiding complex account management.
Output: Secure `/host` route and authenticated WebSocket connection for the operator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-host-control-interface/02-CONTEXT.md
@.planning/phases/02-host-control-interface/02-RESEARCH.md
@server.js
@public/app.js

<interfaces>
From server.js:
```javascript
const io = new Server(server, { ... });
let gameState = {
  drawnNumbers: [],
  lastDrawn: null
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-side Host Authentication Middleware</name>
  <files>server.js, .env</files>
  <action>
    - Add `HOST_SECRET=bingo-secret-123` to `.env`.
    - Implement `io.use` middleware in `server.js` to validate the `token` provided in `socket.handshake.auth`.
    - If `token === process.env.HOST_SECRET`, set `socket.isHost = true`.
    - Ensure non-host connections still work (set `socket.isHost = false`).
    - Remove or comment out the simulation loop in `server.js` that randomly draws numbers.
  </action>
  <verify>
    <automated>
      # Verify middleware by attempting connection with and without token (requires temporary test script or manual node check)
      node -e "const io = require('socket.io-client'); const s = io('http://localhost:3000', { auth: { token: 'wrong' } }); s.on('connect', () => { console.log('Connected'); process.exit(0); });"
    </automated>
  </verify>
  <done>Server rejects/tags connections based on HOST_SECRET</done>
</task>

<task type="auto">
  <name>Task 2: Host Entry Point & Authenticated Connection</name>
  <files>public/host.html, public/host.js</files>
  <action>
    - Create `public/host.html` with basic structure, including a link to `public/host.js`.
    - In `public/host.js`, extract the `key` parameter from `window.location.search`.
    - Initialize Socket.io with the `auth: { token: key }` option.
    - Add a visual indicator in `host.html` showing if the user is "Authenticated as Host" based on a server-emitted confirmation or by checking connection success.
  </action>
  <verify>
    <automated>
      # Check if host.html exists and contains the script reference
      grep "host.js" public/host.html
    </automated>
  </verify>
  <done>Host can connect to the server using the URL key</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server | Untrusted input (HOST_SECRET) crosses here via handshake |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Socket connection | mitigate | Validate token against .env in handshake middleware |
| T-02-02 | Information Disclosure | .env file | mitigate | Ensure .env is gitignored (per standard Node patterns) |
</threat_model>

<verification>
Start server, visit `http://localhost:3000/host?key=bingo-secret-123`, check console for "User connected" and verify that subsequent tasks can use `socket.isHost`.
</verification>

<success_criteria>
1. Server-side validation of host tokens is active.
2. `/host.html` successfully connects with a valid key.
3. Simulation loop is disabled.
</success_criteria>

<output>
After completion, create `.planning/phases/02-host-control-interface/02-01-SUMMARY.md`
</output>
