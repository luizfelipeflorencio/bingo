---
phase: 03-visual-bingo-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [public/index.html, public/style.css, public/app.js]
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03]
must_haves:
  truths:
    - "Participant screen shows a massive current number display at the top (D-01)."
    - "A 1–90 grid highlights all called numbers below the current number (D-02)."
    - "A history strip shows the 5 most recently called numbers (D-03)."
    - "New numbers trigger a 'Pulse & Flash' animation in the display and grid (D-04)."
    - "An empty state is shown before any numbers are drawn (D-05)."
  artifacts:
    - path: "public/index.html"
      provides: "New semantic structure for Top-Heavy layout"
    - path: "public/style.css"
      provides: "Grid layouts, vh-based sizing, and Pulse & Flash keyframes"
    - path: "public/app.js"
      provides: "Grid generation, history slicing logic, and animation triggering"
  key_links:
    - from: "socket.on('numberDrawn')"
      to: "UI update functions"
      via: "Event listener in app.js"
---

<objective>
Transform the participant display into a high-visibility, real-time Bingo board optimized for large screens (projectors).

Purpose: Ensure participants can see calls from a distance and track the game state without refreshing.
Output: Refactored HTML/CSS/JS with 1-90 grid, huge number display, and history strip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-visual-bingo-display/03-CONTEXT.md
@.planning/phases/03-visual-bingo-display/03-RESEARCH.md
@public/index.html
@public/style.css
@public/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structural Refactor & 1-90 Grid Implementation</name>
  <files>public/index.html, public/style.css, public/app.js</files>
  <action>
    - Refactor `public/index.html` to separate the layout into `#current-call-container`, `#history-container`, and `#grid-container`.
    - In `public/style.css`, implement a responsive 1–90 grid using CSS Grid (10 columns). Use `vh` units to ensure the current call container takes up ~40-50% of the vertical space (Top Heavy per D-01).
    - In `public/app.js`, implement a function to generate the 1-90 grid cells on load. Each cell should have a unique ID (e.g., `cell-1`, `cell-2`).
    - Update `renderState` to highlight the corresponding cells in the grid based on `drawnNumbers`.
  </action>
  <verify>
    <automated>
      # Check for 90 grid cells in the DOM via a script
      node -e "const fs = require('fs'); const html = fs.readFileSync('public/index.html', 'utf8'); if (!html.includes('id=\"grid-container\"')) process.exit(1);"
    </automated>
  </verify>
  <done>The 1-90 grid is rendered and correctly highlights drawn numbers from the server state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Huge Number Display & Pulse-Flash Animations</name>
  <files>public/style.css, public/app.js</files>
  <behavior>
    - When a new number is drawn, the `#current-number` element should be updated.
    - The element should receive an `.animate-pulse-flash` class.
    - The corresponding grid cell should also receive the `.animate-pulse-flash` class.
    - The animation should scale the element (1.1x) and flash a highlight color (yellow/green per D-04).
  </behavior>
  <action>
    - Add `@keyframes pulse-flash` to `public/style.css` using `transform: scale()` for GPU acceleration (per research).
    - Update the `numberDrawn` listener in `public/app.js` to:
      1. Update the `#current-number` text.
      2. Apply the animation class to both the huge display and the grid cell.
      3. Use `setTimeout` or `animationend` listener to remove the class so it can be re-triggered.
  </action>
  <verify>
    <automated>
      grep -q "keyframes pulse-flash" public/style.css && grep -q "transform: scale" public/style.css
    </automated>
  </verify>
  <done>New numbers appear in massive font with a visual pulse/flash effect that is synchronized between the main display and the grid.</done>
</task>

<task type="auto">
  <name>Task 3: History Strip & Empty State Handling</name>
  <files>public/app.js, public/index.html, public/style.css</files>
  <action>
    - Update `public/app.js` to implement `getLatestHistory(drawnNumbers)` which returns the last 5 numbers *excluding* the most recent one (per D-03).
    - Render these numbers in the `#history-container` as a horizontal list.
    - Implement the empty state (D-05): if `drawnNumbers` is empty, show "Waiting for game to start..." in the huge display and dim the grid cells.
    - Final CSS polish: ensure high contrast (white on dark) for maximum legibility (per Pitfall 1).
  </action>
  <verify>
    <automated>
      # Verify history slice logic exists in app.js
      grep -q "slice" public/app.js
    </automated>
  </verify>
  <done>History strip correctly displays previous 5 calls; empty state provides clear feedback before the game starts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client | Socket.io events (numberDrawn, SYNC) are trusted but should be validated on receipt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Participant View | Mitigate | Ensure no host tokens or sensitive state are leaked in the SYNC payload (already handled by separate auth). |
| T-03-02 | Denial of Service | Browser Rendering | Mitigate | Limit animation duration and frequency to prevent GPU lag on low-end hardware. |
</threat_model>

<verification>
1. Open participant view; verify 1-90 grid is visible and dimmed.
2. Huge display shows "Waiting for game to start...".
3. Use host panel to draw a number; verify huge display updates and grid cell highlights with a pulse animation.
4. Draw 6 numbers; verify the history strip shows exactly numbers 1-5 (if 6 is current).
</verification>

<success_criteria>
- DISP-01: Huge current number is prominently displayed.
- DISP-02: 1-90 grid highlights all called numbers correctly.
- DISP-03: History bar shows the last 5 numbers in order.
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-bingo-display/03-01-SUMMARY.md`
</output>
