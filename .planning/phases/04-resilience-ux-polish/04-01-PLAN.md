---
phase: 04-resilience-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, server.js, public/app.js, public/host.js, public/style.css]
autonomous: true
requirements: [SYNC-02, D-01, D-03, D-04]

must_haves:
  truths:
    - "Server state survives process restart via gameState.json"
    - "Client UI state survives browser refresh via LocalStorage"
    - "Host UI is usable on small touch screens (safe areas, large targets)"
    - "Participant view scales from mobile to projector without layout breakage"
  artifacts:
    - path: "gameState.json"
      provides: "Persistent server state"
    - path: "server.js"
      provides: "Atomic state saving with throttling"
    - path: "public/style.css"
      provides: "Responsive design for mobile and projector"
  key_links:
    - from: "server.js"
      to: "gameState.json"
      via: "fs.writeFileSync"
    - from: "public/app.js"
      to: "localStorage"
      via: "JSON.stringify/parse"

---

<objective>
Implement state persistence for both server and client to ensure resilience against restarts and refreshes, and polish the UI for responsive display across various devices (mobile to projector).

Purpose: Ensure a robust user experience where game progress is never lost and the UI is accessible on all intended hardware.
Output: Persistent server state, hardened client sync, and responsive CSS updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@server.js
@public/app.js
@public/host.js
@public/style.css
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-side Persistence Foundation</name>
  <files>package.json, server.js</files>
  <action>
    - Install `lodash.throttle` to manage write frequency to disk.
    - Implement `saveStateAtomic` in `server.js` using `fs.writeFileSync` (atomic write pattern: write to temp file then rename).
    - Use `lodash.throttle` (e.g., 1000ms) to wrap the save function to prevent disk thrashing on rapid draws.
    - Load `gameState.json` on server startup; if missing, initialize with default empty state.
    - Ensure `gameState.json` is ignored by nodemon to avoid restart loops.
    - Per D-01.
  </action>
  <verify>
    <automated>npm install lodash.throttle && node -e "const fs = require('fs'); const state = {drawnNumbers: [1,2], lastDrawn: 2}; fs.writeFileSync('gameState.json', JSON.stringify(state)); require('./server.js'); console.log('Server started and loaded state');"</automated>
  </verify>
  <done>Server loads state from disk and throttles saves on state changes.</done>
</task>

<task type="auto">
  <name>Task 2: Client-side Hardening & LocalStorage Sync</name>
  <files>public/app.js, public/host.js</files>
  <action>
    - Hardened `saveToLocal` and `loadFromLocal` in `app.js` and `host.js` with try/catch blocks.
    - Ensure `host.js` also implements LocalStorage persistence for the drawn numbers grid to prevent flickering on refresh.
    - Implement a "version check" or timestamp in the state to ensure `SYNC` events from server always override older local state.
    - Per SYNC-02.
  </action>
  <verify>
    <automated># Manual verification in browser console: localStorage.getItem('bingo_game_state') should match server SYNC payload</automated>
  </verify>
  <done>Clients reliably persist and sync state across refreshes.</done>
</task>

<task type="auto">
  <name>Task 3: Responsive UI Polish & Mobile UX</name>
  <files>public/style.css, public/host.html</files>
  <action>
    - Update `public/style.css` to use `env(safe-area-inset-*)` for modern mobile displays.
    - Implement `aspect-ratio` based scaling for the `current-number` to ensure it fits both 16:9 projectors and 9:16 mobile screens.
    - Increase `num-btn` touch targets in `host.html`/`style.css` for easier mobile usage (minimum 44x44px).
    - Add `touch-action: manipulation` to buttons to prevent double-tap zoom delays.
    - Per D-03 and D-04.
  </action>
  <verify>
    <automated># Manual check: open host.html in mobile emulation; buttons should be large and easy to tap.</automated>
  </verify>
  <done>UI is responsive and mobile-friendly with safe-area support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Server → Disk | Writing `gameState.json` involves local I/O. |
| Client → LocalStorage | Browser storage is local but user-accessible. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | gameState.json | mitigate | Ensure gameState.json is not served as a static asset by Express. |
| T-04-02 | Denial of Service | Disk I/O | mitigate | Use lodash.throttle to prevent disk thrashing. |
| T-04-03 | Tampering | LocalStorage | accept | Game is casual; server is authoritative. Local tampering only affects the individual user. |
</threat_model>

<verification>
- Start server, draw numbers, restart server: numbers should still be there.
- Open participant/host, draw numbers, refresh browser: UI should immediately show correct state before socket connects.
- View UI on mobile and projector resolutions: layout should remain functional and clear.
</verification>

<success_criteria>
- Game state persists across server restarts.
- UI state persists across browser refreshes.
- No "disk thrashing" (multiple writes per second) on the server.
- Host UI passes basic mobile usability checks (touch target size).
</success_criteria>

<output>
After completion, create `.planning/phases/04-resilience-ux-polish/04-01-SUMMARY.md`
</output>
